/**
 * This function greets user via console.log
 * 
 * @param name User's name
 * @returns Greeting message
 * 
 * example
 * ```js
 * hello('Mithila Palkar')
 * ```
 *  */
export function hello (name: string): string {
  console.log(`Hello ${name} 👋🏻`)

  return `Hello ${name} 👋🏻`
}